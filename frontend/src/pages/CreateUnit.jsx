import { useNavigate,useParams } from 'react-router-dom'; import api from '../api/axios'; import SimpleForm from './SimpleForm';
export default function CreateUnit(){const {classId}=useParams(),navigate=useNavigate();return <SimpleForm title="Create a unit" subtitle="Group related materials and assessments into a clear learning sequence." initial={{title:'',description:'',order_number:1}} fields={[{name:'title',label:'Unit title',required:true},{name:'description',label:'Description',type:'textarea'},{name:'order_number',label:'Order number',type:'number',min:1,required:true}]} submitLabel="Create unit" onSubmit={async data=>{await api.post(`/classrooms/${classId}/units`,data);navigate(`/classes/${classId}`)}}/>}
