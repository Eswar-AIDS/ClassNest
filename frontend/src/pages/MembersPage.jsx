import { useEffect,useState } from 'react'; import { Link,useParams } from 'react-router-dom'; import { ArrowLeft,Users } from 'lucide-react'; import api from '../api/axios';
export default function MembersPage(){const {classId}=useParams(),[members,setMembers]=useState([]),[room,setRoom]=useState(null);useEffect(()=>{Promise.all([api.get(`/classrooms/${classId}`),api.get(`/classrooms/${classId}/members`)]).then(([a,b])=>{setRoom(a.data);setMembers(b.data)})},[classId]);return <><Link to={`/classes/${classId}`} className="mb-5 flex items-center gap-2 text-sm font-semibold text-slate-500"><ArrowLeft size={17}/>Back to class</Link><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-brand-50 text-brand-600"><Users/></span><div><h1 className="page-title">Class members</h1><p className="text-slate-500">{room?.name}</p></div></div><div className="card mt-7 overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Name</th><th className="px-5 py-3">Email</th><th className="px-5 py-3">Role in class</th><th className="px-5 py-3">Joined</th></tr></thead><tbody className="divide-y">{members.map(m=><tr key={m.id}><td className="px-5 py-4 font-semibold">{m.name}</td><td className="px-5 py-4 text-slate-500">{m.email || 'Hidden'}</td><td className="px-5 py-4 capitalize">{m.role}</td><td className="px-5 py-4 text-slate-500">{new Date(m.joined_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></div></>}
